# DishRoller-App-ios-dev

This project includes GenAI recipe generation.

For security reasons, the API key is not included in this repository.

To run the AI feature:
1. Copy Config.example.plist
2. Rename it to Config.plist
3. Replace YOUR_API_KEY_HERE with your own API key
4. Run the app in Xcode
